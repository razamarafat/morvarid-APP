
# 🚀 سامانه مدیریت یکپارچه آمار مروارید (Morvarid Integrated Statistics System)

این یک سامانه پیشرفته، امن و واکنش‌گرا برای مدیریت یکپارچه آمار تولید، فروش و انبارداری فارم‌های پرورش طیور است که با استانداردهای طراحی **Metro UI** و به صورت **PWA (Progressive Web App)** پیاده‌سازی شده است.

---

## ✨ ویژگی‌های کلیدی (Key Features)

- **📱 اپلیکیشن پیش‌رونده (PWA)**: قابلیت نصب بر روی اندروید و iOS با پشتیبانی کامل از حالت آفلاین.
- **🛡️ مدیریت سطوح دسترسی**: تفکیک وظایف بین مدیر (Admin)، مسئول ثبت (Registration) و مسئول فروش (Sales).
- **📊 پایش آنی (Real-time Monitoring)**: مشاهده لحظه‌ای آمار تولید و موجودی انبار تمام فارم‌ها.
- **⚡ سیستم هشدار هوشمند**: ارسال هشدارهای آنی (Broadcast) در صورت عدم ثبت آمار یا بروز وضعیت بحرانی.
- **📄 گزارش‌گیری اکسل**: خروجی هوشمند داده‌ها در قالب فایل‌های XLSX با پشتیبانی از تاریخ شمسی و راست‌چین.
- **🌑 تم دوگانه (Dark/Light)**: بهینه‌سازی شده برای شرایط نوری مختلف فارم‌ها.

---

## 🛠️ پشته تکنولوژی (Tech Stack)

- **Frontend**: React 19, TypeScript, Tailwind CSS
- **State Management**: Zustand
- **Backend/Database**: Supabase (PostgreSQL + Realtime)
- **Animation**: Framer Motion
- **Form Handling**: React Hook Form + Zod
- **Excel Processing**: SheetJS (XLSX)

---

## 🚀 راه اندازی سریع (Quick Setup)

**مهم: تنظیم متغیرهای محیطی (Environment Variables)**

این پروژه برای اجرا نیاز به اتصال به دیتابیس Supabase دارد. برای امنیت، فایل `.env` در مخزن وجود ندارد.

1. **کپی فایل الگو**:
   فایل `.env.example` را کپی کرده و نام آن را به `.env` تغییر دهید:
   ```bash
   cp .env.example .env
   ```

2. **وارد کردن کلیدها**:
   فایل `.env` را باز کنید و مقادیر زیر را با اطلاعات پروژه Supabase خود جایگزین کنید:
   ```env
   VITE_SUPABASE_URL=https://your-project-id.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key
   ```

3. **نصب وابستگی‌ها**:
   ```bash
   npm install
   ```

4. **اجرا در حالت توسعه**:
   ```bash
   npm run dev
   ```
   *(نکته: اگر فایل `.env` وجود نداشته باشد، اسکریپت امنیتی جلوی اجرای برنامه را می‌گیرد).*

5. **ساخت نسخه نهایی**:
   ```bash
   npm run build
   ```

---

## 📂 ساختار پوشه‌بندی (Folder Structure)

- `components/`: اجزای مشترک و اختصاصی UI (دکمه‌ها، مودال‌ها، سایدبار و ...)
- `store/`: مدیریت وضعیت سراسری برنامه (Zustand Stores)
- `hooks/`: هوک‌های سفارشی برای منطق برنامه (تاییدیه، بایومتریک، پیامک)
- `pages/`: صفحات اصلی برنامه بر اساس نقش کاربران
- `utils/`: توابع کمکی برای مدیریت تاریخ شمسی و پردازش داده‌ها
- `lib/`: پیکربندی کتابخانه‌های خارجی (Supabase)
- `scripts/`: اسکریپت‌های کمکی (مانند چک کردن محیط)

---

## 📝 لایسنس (License)

این پروژه تحت لایسنس اختصاصی مروارید توسعه یافته است. تمام حقوق برای توسعه‌دهندگان محفوظ است.
