import { motion, AnimatePresence } from 'framer-motion';

// Direct exports from framer-motion (will be lazy loaded at the chunk level)
export const MotionDivWrapper = motion.div;
export const MotionSpanWrapper = motion.span;
export const MotionButtonWrapper = motion.button;
export const AnimatePresenceWrapper = AnimatePresence;
